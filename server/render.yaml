services:
  - type: web
    name: fastapi-server
    env: python
    rootDir: .
    buildCommand: |
      pip install uv
      uv sync --frozen
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
